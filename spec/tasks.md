# Lista de Tarefas: Encurtamento de Links

- [x] 1. Criar o novo utilitário `lib/urlUtils.ts` contendo a função `shortenUrl` para encurtar a exibição de URLs.
- [x] 2. Modificar a função `linkify` em `lib/linkUtils.tsx` para que ela utilize a nova função `shortenUrl` para o texto do link.
